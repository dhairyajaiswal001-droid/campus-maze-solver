# campus-maze-solver
A Python-based pathfinding agent navigating a 2D campus grid. Compares BFS (Uninformed) and A (Informed)* search strategies using the f(n) = g(n) + h(n) evaluation function and Manhattan Distance to find the optimal path from Hostel to Lab.
